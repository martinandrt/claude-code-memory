# Optional semantic-recall layer only. The core memory system needs none of this.
# Install in a venv: python3 -m venv scripts/recall/.venv && .venv/bin/pip install -r requirements.txt
sentence-transformers>=2.7
numpy>=1.24
